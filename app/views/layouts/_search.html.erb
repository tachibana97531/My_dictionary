<% if user_signed_in? %>
  <div class="search_form">
    <%= form_with url: search_path, local: true, method: :get do |f| %>
      <%= f.text_field :word,placeholder:"辞書を検索" %>
      <%= f.submit "検索",class:"btn btn-sm btn-success" %>
    <% end %>
  </div>
<% end %>